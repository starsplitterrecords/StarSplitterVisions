export interface FeaturedSeriesItem {
  slug: string
  title: string
  issue: string
  hook: string
  cover: string
}

export interface MoreWorldItem {
  slug: string
  title: string
  cover: string
}
